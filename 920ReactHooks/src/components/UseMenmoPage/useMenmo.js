import React from "react";

function UseMenmos(){
    return (
        <div>
           <h2>useMenmo解决子组件重复执行的问题</h2> 
            <p>useMemo 用于性能优化，通过记忆值来避免在每个渲染上执⾏高
            开销的计算。</p>
        </div>
    )
};

export default UseMenmos;